# Template only: copy values into .env.local (or .env.development.local)
# Do not put secrets directly in tracked files.
VITE_API_BASE_URL=http://localhost:8080
VITE_KEYCLOAK_URL=http://localhost:8090
VITE_KEYCLOAK_REALM=the_keycloak_realm
VITE_KEYCLOAK_CLIENT_ID=the_keycloak_client_id
VITE_KEYCLOAK_LOGOUT_REDIRECT_URI=http://localhost:5173
